# .env.example is meant to be checked in with no sensitive values so that one can easily deploy the lambda

ENV=dev

# AWS env vars. If you are using ~/.aws, no need to set values. Values set here will have precedence over ~/.aws when using aws cli
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
AWS_SESSION_TOKEN
AWS_PROFILE
